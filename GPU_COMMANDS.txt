================================================================================
          CAPSTONE-MEDSEG: GPU EXECUTION & GIT FIX CHEATSHEET
================================================================================

================================================================================
CRITICAL: HOW TO FIX GIT ERRORS & PUSH ALL RESULTS (30 & 40 EPOCHS)
================================================================================

If you see:
1. "The following paths are ignored... Use -f if you really want to add them"
   --> That is because .pt files are in .gitignore. You must use `git add -f` for .pt files!
2. "Updates were rejected because a pushed branch tip is behind its remote"
   --> That is because the Mac pushed updates. You must use `git pull origin main --rebase`!

--------------------------------------------------------------------------------
>>> RUN THIS EXACT BLOCK TO PUSH ALL YOUR RESULTS (BOTH 30 & 40 EPOCHS):
--------------------------------------------------------------------------------
Copy and paste these 5 lines into PowerShell on the GPU PC:

   git add logs/
   git add -f models/checkpoints/*.pt
   git commit -m "completed 30-epoch and 40-epoch training and evaluation results"
   git pull origin main --rebase
   git push origin main

Once this succeeds, everything (all logs, tables, plots, and .pt checkpoints) will be on GitHub!


================================================================================
HOW TO VIEW YOUR RESULTS RIGHT ON THE SCREEN (POWERSHELL)
================================================================================

1. View the latest episodic zero-shot results table:
   Get-Content logs\episodic_zeroshot_results.txt

2. View the last 30 lines of the training log (to see latest epoch):
   Get-Content logs\episodic_training.txt -Tail 30

3. View LODO (Leave-One-Out) results if completed:
   Get-Content logs\episodic_lodo_spleen.txt
   Get-Content logs\episodic_lodo_liver.txt
   Get-Content logs\episodic_lodo_heart.txt
   Get-Content logs\episodic_lodo_braintumour.txt

4. View 30-epoch shot ablation results:
   Get-Content logs\shot_ablation_results.txt


================================================================================
IF TRAINING IS STILL RUNNING:
================================================================================
Watch it live in PowerShell:
   Get-Content logs\episodic_training.txt -Wait -Tail 15

If it stopped early or was interrupted, resume seamlessly:
   python -u src\train_episodic.py --epochs 40 --episodes_per_epoch 500 --lr 5e-4 --resume


================================================================================
NEXT GPU EXPERIMENTS TO RUN (AFTER 40 EPOCHS):
================================================================================

A) LEAVE-ONE-DATASET-OUT (LODO) MATRIX (Takes ~45 mins):
   python run_gpu_experiments.py --only_lodo

   When LODO finishes, push results:
   git add logs/
   git add -f models/checkpoints/*.pt
   git commit -m "completed LODO experiments"
   git pull origin main --rebase
   git push origin main

B) MULTI-SEED RUNS (Seeds 123 & 456):
   # Seed 123:
   python -u src\train_episodic.py --epochs 30 --episodes_per_epoch 500 --lr 5e-4 --num_layers 3 --seed 123 --checkpoint models\checkpoints\best_fusion_episodic_seed123.pt --results_file logs\episodic_zeroshot_results_seed123.txt

   # Seed 456:
   python -u src\train_episodic.py --epochs 30 --episodes_per_epoch 500 --lr 5e-4 --num_layers 3 --seed 456 --checkpoint models\checkpoints\best_fusion_episodic_seed456.pt --results_file logs\episodic_zeroshot_results_seed456.txt

   When multi-seed finishes, push results:
   git add logs/
   git add -f models/checkpoints/*.pt
   git commit -m "completed multi-seed runs 123 and 456"
   git pull origin main --rebase
   git push origin main


================================================================================
QUICK EMERGENCY: IF GIT STILL COMPLAINS
================================================================================
If git rebase gets stuck or prompts you:
   git rebase --abort
   git stash
   git pull origin main
   git stash pop
   git add logs/
   git add -f models/checkpoints/*.pt
   git commit -m "push all results"
   git push origin main
================================================================================
